# Skills

Place **reusable agent skills** here: short, self-contained instructions the model can load for a specific capability (e.g. “add OpenTelemetry span around pgx call”, “write a migration safely”).

This repo seeds the folder only; add `*.md` files as you grow the template.
